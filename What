-- Get the local player
local player = game.Players.LocalPlayer

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AutoUpgradesGUI"
screenGui.Parent = player.PlayerGui

-- Helper to create toggle buttons
local function CreateToggleButton(text, position)
    local button = Instance.new("TextButton")
    button.Size = UDim2.new(0, 220, 0, 50)
    button.Position = position
    button.AnchorPoint = Vector2.new(0.5, 0.5)
    button.Text = text..": OFF"
    button.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    button.TextColor3 = Color3.new(1,1,1)
    button.Font = Enum.Font.SourceSansBold
    button.TextSize = 18
    button.Parent = screenGui
    return button
end

-- Buttons
local swordButton = CreateToggleButton("Auto Sword", UDim2.new(0.5, 0, 0.4, -60))
local classButton = CreateToggleButton("Auto Class", UDim2.new(0.5, 0, 0.5, -60))
local realmButton = CreateToggleButton("Auto Realm", UDim2.new(0.5, 0, 0.6, -60))

-- State variables
local autoSword, autoClass, autoRealm = false, false, false

-- Function templates
local function AutoSword()
    local remoteService = game:WaitForChild("ClientStorage"):WaitForChild("Client"):WaitForChild("Services"):WaitForChild("PersonalRemoteService")
    local serverRemote = game:GetService("ServerScriptService"):WaitForChild("f71a33bb-67ed-4740-bb24-251f71c763a1")
    local args = { { remoteService, "72475603-84f3-4f05-89e4-2a1481c09f2c", "0.658061165515571", "d6bab4b9-7047-48d3-9922-d8cdeedf7ee3" } }
    
    while autoSword do
        pcall(function()
            serverRemote:InvokeServer(unpack(args))
        end)
        wait(0.2)
    end
end

local function AutoClass()
    local remoteService = game:WaitForChild("ClientStorage"):WaitForChild("Client"):WaitForChild("Services"):WaitForChild("PersonalRemoteService")
    local serverRemote = game:GetService("UserInputService"):WaitForChild("c98b3a90-7b35-430c-9fbb-b7cde7c161f1")
    local args = { { remoteService, "08067edb-d83c-4e64-a123-03bfc4823bdc", "0.1404403466134245", "9c6d2024-4a1e-41d3-b069-58eb0c1547b7" } }
    
    while autoClass do
        pcall(function()
            serverRemote:InvokeServer(unpack(args))
        end)
        wait(2)
    end
end

local function AutoRealm()
    local remoteService = game:WaitForChild("ClientStorage"):WaitForChild("Client"):WaitForChild("Services"):WaitForChild("PersonalRemoteService")
    local serverRemote = game:GetService("PhysicsService"):WaitForChild("bcc29e2a-61d5-4217-9229-1f0698a30646")
    local args = { { remoteService, "5f2d4a48-03c4-42f5-9938-24c998d7fb74", "0.8957692338536427", "384fc7f2-8f79-4ac9-b013-4b8b1ae71009" } }
    
    while autoRealm do
        pcall(function()
            serverRemote:InvokeServer(unpack(args))
        end)
        wait(3)
    end
end

-- Toggle functions
swordButton.MouseButton1Click:Connect(function()
    autoSword = not autoSword
    swordButton.Text = "Auto Sword: "..(autoSword and "ON" or "OFF")
    if autoSword then spawn(AutoSword) end
end)

classButton.MouseButton1Click:Connect(function()
    autoClass = not autoClass
    classButton.Text = "Auto Class: "..(autoClass and "ON" or "OFF")
    if autoClass then spawn(AutoClass) end
end)

realmButton.MouseButton1Click:Connect(function()
    autoRealm = not autoRealm
    realmButton.Text = "Auto Realm: "..(autoRealm and "ON" or "OFF")
    if autoRealm then spawn(AutoRealm) end
end)
